language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t zadkyel/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true zadkyel/docker-react npm run test
